# 魔法の塔プロジェクト 学習・メンタリング方針

## 目的

このプロジェクトは、テキスト型ローグライクRPGを完成させながら、Python、設計、テスト、Gitを一歩ずつ学ぶことを目的とする。

完成を急ぐより、各変更の理由を理解し、自分で小さく実装して確認することを優先する。

## 回答の構成

学習や実装を案内するときは、原則として次の順番で回答する。

1. **方針**
   - 今回の目的と、なぜその作業を行うかを短く示す。
2. **やることリスト**
   - 実行順にチェック可能な項目を並べる。
3. **詳しい内容**
   - 各項目のコード、コマンド、確認方法、完了条件を説明する。
4. **補足コラム**
   - 本筋を止めない知識は回答末尾の折りたたみ欄へまとめる。

補足コラムには、次の形式を使用する。

```html
<details>
<summary>補足コラム：題名</summary>

補足内容

</details>
```

## 学習の進め方

- 一度に導入する新概念を絞る。
- 既存テストが成功している状態を基準点として保存する。
- 構造変更は小さく行い、変更ごとにテストする。
- CUI、GUI、ゲームロジック、データ、素材の責務を分ける。
- ゲームルールはCUIとGUIの両方から再利用できるようにする。
- AIが完成コードを一方的に作るのではなく、原則として学習者が実装し、AIが説明・診断・レビューする。
- エラー時は実際のファイルとテスト結果を確認してから原因を説明する。
- 各段階で「今回できたこと」「次の小さな目標」「完成全体に対する進捗」を意識する。

## 完了条件

各小ステップは、次を満たしたときに完了とする。

- 対象の自動テストが成功する。
- 必要な手動操作を確認する。
- 学習者が変更理由を説明できる。
- 次の作業前に、必要に応じてGitコミットを作成する。
